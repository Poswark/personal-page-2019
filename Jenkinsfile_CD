pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                echo 'Deploy..'
                echo 'Ingresando al servidor y desplegando en el cluster.'

            }
        }
        
    }
}